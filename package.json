{
  "name": "02-weather",
  "version": "1.0.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
	"build": "bun test && bun build --compile src/index.ts --outfile weather-cli",
	"dev": "bun run src/index.ts --watch",
	"start": "bun run src/index.ts",
	"test": "bun test",
	"test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
